# F-Droid integration

This repo supports an F-Droid distribution channel selected via `PEARPASS_DISTRIBUTION=fdroid`.

`scripts/fdroid-build.sh [prebuild|build|all] [abi]` runs the prebuild and Gradle patching steps the F-Droid recipe needs.
